<%= form_for(current_user.relationships.find_by_followed_id(@kpiset),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Untrack", class: "btn btn-mini btn-primary" %>
<% end %>